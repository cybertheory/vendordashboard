// vendor-dashboard/next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Add any specific Next.js configurations here if needed.
  // For example, if you want strict mode:
  // reactStrictMode: true,
};

export default nextConfig; // Use ES Module export